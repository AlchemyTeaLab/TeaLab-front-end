export const signIn = ({ email, password }) => {
  // fetch post
};

export const signUp = ({ email, password, username }) => {
  // fetch post
};

export const signOut = () => {
  // fetch delete
};
